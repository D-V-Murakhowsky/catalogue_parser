import pandas as pd
from typing import Tuple


class Synchronizer:

    @staticmethod
    def sync_tables(supplier_data: pd.DataFrame,
                    google_sheet_data: pd.DataFrame) -> Tuple[pd.DataFrame]:

        pass
